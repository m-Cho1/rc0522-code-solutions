/* exported head */

// input: any JS array
// output: the first element of the input array

// - access the first element of the array by the index.
// - return the first index of the array as a return value.

function head(array) {
  return array[0];
}
