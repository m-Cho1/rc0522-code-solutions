/* exported last */

// input: any JS array
// output: the last element of the array

// - access the last element of the input array by the index.
// - return the last index of the array  by calculating the length of the array and subtract 1
// - return the array at last index as a return value.

function last(array) {
  return array[array.length - 1];
}
